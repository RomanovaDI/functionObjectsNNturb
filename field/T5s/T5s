// Copy this file to system/ folder and run
// postProcess -func T5s

T5s
{
    type            readFields;
    libs            (fieldFunctionObjects);
    fields          (U);
    type            T5s;
    libs            (fieldNNturbFunctionObjects);
    executeControl  writeTime;
    writeControl    writeTime;
}
